hft:
  mode: LIVE
  executor:
    base-url: ${POLYBOT_EXECUTOR_BASE_URL:http://localhost:8080}
  polymarket:
    market-ws-enabled: true
  strategy:
    house-edge:
      enabled: true
      discovery:
        enabled: true
        queries: [ Bitcoin, Ethereum, Solana, XRP ]
